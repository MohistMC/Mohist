--- a/net/minecraft/world/item/LeadItem.java
+++ b/net/minecraft/world/item/LeadItem.java
@@ -11,6 +_,9 @@
 import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.level.gameevent.GameEvent;
 import net.minecraft.world.phys.AABB;
+import org.bukkit.craftbukkit.v1_20_R1.CraftEquipmentSlot;
+import org.bukkit.craftbukkit.v1_20_R1.block.CraftBlock;
+import org.bukkit.event.hanging.HangingPlaceEvent;
 
 public class LeadItem extends Item {
    public LeadItem(Item.Properties p_42828_) {
@@ -24,7 +_,7 @@
       if (blockstate.m_204336_(BlockTags.f_13039_)) {
          Player player = p_42834_.m_43723_();
          if (!level.f_46443_ && player != null) {
-            m_42829_(player, level, blockpos);
+            bindPlayerMobs(player, level, blockpos, p_42834_.m_43724_()); // CraftBukkit - Pass hand
          }
 
          return InteractionResult.m_19078_(level.f_46443_);
@@ -33,30 +_,53 @@
       }
    }
 
-   public static InteractionResult m_42829_(Player p_42830_, Level p_42831_, BlockPos p_42832_) {
+   public static InteractionResult bindPlayerMobs(Player pPlayer, Level pLevel, BlockPos pPos, net.minecraft.world.InteractionHand enumhand) {
       LeashFenceKnotEntity leashfenceknotentity = null;
       boolean flag = false;
       double d0 = 7.0D;
-      int i = p_42832_.m_123341_();
-      int j = p_42832_.m_123342_();
-      int k = p_42832_.m_123343_();
+      int i = pPos.m_123341_();
+      int j = pPos.m_123342_();
+      int k = pPos.m_123343_();
 
-      for(Mob mob : p_42831_.m_45976_(Mob.class, new AABB((double)i - 7.0D, (double)j - 7.0D, (double)k - 7.0D, (double)i + 7.0D, (double)j + 7.0D, (double)k + 7.0D))) {
-         if (mob.m_21524_() == p_42830_) {
+      for(Mob mob : pLevel.m_45976_(Mob.class, new AABB((double)i - 7.0D, (double)j - 7.0D, (double)k - 7.0D, (double)i + 7.0D, (double)j + 7.0D, (double)k + 7.0D))) {
+         if (mob.m_21524_() == pPlayer) {
             if (leashfenceknotentity == null) {
-               leashfenceknotentity = LeashFenceKnotEntity.m_31844_(p_42831_, p_42832_);
+               leashfenceknotentity = LeashFenceKnotEntity.m_31844_(pLevel, pPos);
+
+               // CraftBukkit start - fire HangingPlaceEvent
+               org.bukkit.inventory.EquipmentSlot hand = CraftEquipmentSlot.getHand(enumhand);
+               HangingPlaceEvent event = new HangingPlaceEvent((org.bukkit.entity.Hanging) leashfenceknotentity.getBukkitEntity(), pPlayer != null ? (org.bukkit.entity.Player) pPlayer.getBukkitEntity() : null, CraftBlock.at(pLevel, pPos), org.bukkit.block.BlockFace.SELF, hand);
+               pLevel.getCraftServer().getPluginManager().callEvent(event);
+
+               if (event.isCancelled()) {
+                  leashfenceknotentity.m_146870_();
+                  return InteractionResult.PASS;
+               }
+               // CraftBukkit end
                leashfenceknotentity.m_7084_();
             }
 
+            // CraftBukkit start
+            if (org.bukkit.craftbukkit.v1_20_R1.event.CraftEventFactory.callPlayerLeashEntityEvent(mob, leashfenceknotentity, pPlayer, enumhand).isCancelled()) {
+               continue;
+            }
+            // CraftBukkit end
+
             mob.m_21463_(leashfenceknotentity, true);
             flag = true;
          }
       }
 
       if (flag) {
-         p_42831_.m_220407_(GameEvent.f_157791_, p_42832_, GameEvent.Context.m_223717_(p_42830_));
+         pLevel.m_220407_(GameEvent.f_157791_, pPos, GameEvent.Context.m_223717_(pPlayer));
       }
 
       return flag ? InteractionResult.SUCCESS : InteractionResult.PASS;
    }
+
+   // CraftBukkit start
+   public static InteractionResult m_42829_(Player p_42830_, Level p_42831_, BlockPos p_42832_) {
+      return bindPlayerMobs(p_42830_, p_42831_, p_42832_, net.minecraft.world.InteractionHand.MAIN_HAND);
+   }
+   // CraftBukkit end
 }
